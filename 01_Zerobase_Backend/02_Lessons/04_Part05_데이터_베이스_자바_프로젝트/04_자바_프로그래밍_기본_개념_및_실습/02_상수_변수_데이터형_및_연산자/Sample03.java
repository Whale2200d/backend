public class Sample03 {
    public void test() {
        // 단항연산자(피연산자: 1개)
        // 이항연산자(피연산자: 2개)
        // 삼항연산자(피연산자: 3개)

        // 단항 연산자: ++, --, +, -, ~, !
        // 산술 연산자: +, -, *, /, %
        // 비교 연산자: ==, !=, >, <, >=, <=
        // 논리 연산자: &&(and), ||(or), !(not)
        // 비트 연산자: &, |, ^, ~
        // 삼항 연산자: ?: (조건식) ? 참일 때 값 : 거짓일 때 값
        // 대입 연산자: =, +=, -=, *=, /=, %=
    }
}
